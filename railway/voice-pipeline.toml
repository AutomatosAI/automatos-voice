[build]
builder = "DOCKERFILE"
dockerfilePath = "services/voice-pipeline/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
sleepApplication = false

[service]
internalPort = 8301
